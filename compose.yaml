services:
  license-api:
    build: .
    container_name: twilight-license-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - license_data:/data
    environment:
      DATABASE_PATH: /data/licenses.db

volumes:
  license_data:
